{
  "name": "vexiqo-whatsapp-webhook",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "npx prisma db push && node index.js",
    "postinstall": "npx prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0",
    "express": "^4.19.2",
    "openai": "^4.0.0",
    "prisma": "^6.0.0",
    "playwright": "^1.42.0",
    "pdfkit": "^0.16.0"
  },
"scripts": {
  "start": "npx prisma db push && node index.js",
  "postinstall": "npx playwright install --with-deps chromium"
}
  
}

